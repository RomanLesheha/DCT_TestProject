<Window x:Class="DCT_TestProject.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"

        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid x:Name="ChartGrid" RenderTransformOrigin="0.551,0.499" Margin="10,0,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="11"/>
            <ColumnDefinition Width="12*"/>
            <ColumnDefinition Width="110*"/>
            <ColumnDefinition Width="141*"/>
        </Grid.ColumnDefinitions>

        <Button x:Name="Currency_Converter" Content="Currency Converter" HorizontalAlignment="Left" Margin="0,6,0,0" VerticalAlignment="Top" Height="43" Width="114" RenderTransformOrigin="0.5,0.5" Click="Button1_Click" Grid.Column="2">
            <Button.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform/>
                    <RotateTransform Angle="0.087"/>
                    <TranslateTransform/>
                </TransformGroup>
            </Button.RenderTransform>
        </Button>
        <DataGrid Grid.ColumnSpan="2"/>
        <lvc:CartesianChart Series="{Binding SeriesCollection}" LegendLocation="Left" Margin="1,60,10,105" Grid.Column="2" DataClick="Chart_Click">
            <lvc:CartesianChart.AxisX>
                <lvc:Axis Title="Currency Name" Labels="{Binding Labels}"></lvc:Axis>
            </lvc:CartesianChart.AxisX>
            <lvc:CartesianChart.AxisY>
                <lvc:Axis Title="Price (USD)" LabelFormatter="{Binding Formatter}"></lvc:Axis>
            </lvc:CartesianChart.AxisY>
        </lvc:CartesianChart>

    </Grid>

</Window>
